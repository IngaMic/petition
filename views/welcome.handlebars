<div>
    <h1>Petition</h1>
    <p>About something something</p>
</div>
<div>
    <form method='POST' action="/login">
        <input type='text' name='first' placeholder='First Name' autocomplete='off'>
        <input type='text' name='last' placeholder='Last Name' autocomplete='off'>
        <div>
            {{!-- type="hidden"  --}}
            {{!-- <input name="accept"> --}}
            {{!-- figure out the < error --}}

            <input type="hidden" name="_csrf" value="{{csrfToken}}">
            <canvas name="signature" id="canvas" height="230" width="380" style="border: 2px solid black"></canvas>
        </div>
        <button name="submit" value="submit"> Submit </submit>
    </form>
</div>